# Newly added function

`update_probability_map` function was added to `policy.py` file and some new lines were added to `main_start.py`
Screenshots of new outputs were added too `screenshots_of_new_labtask` folder and previous lab's outputs are in `screenshots_of_previouslab_outputs` folder.
---
